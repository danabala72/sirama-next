import{Card,EmptyRow,Notice,PageHeader,buttonClass,dangerClass,inputClass}from"@/components/admin-ui";import{requireUser}from"@/lib/auth";import{prisma}from"@/lib/prisma";import{deleteAdminJurusan,saveAdminJurusan}from"../admin-actions";
export default async function Page({searchParams}:{searchParams:Promise<{notice?:string}>}){await requireUser(["Admin"]);const{notice}=await searchParams;const[rows,jurusan]=await Promise.all([prisma.adminJurusan.findMany({include:{user:{include:{jurusan:true}}},orderBy:{nama:"asc"}}),prisma.jurusan.findMany({orderBy:{namaJurusan:"asc"}})]);return <><PageHeader title="Admin Jurusan" description="Setiap admin hanya dapat mengelola data pada jurusan akunnya."/><Notice text={notice}/><Card><form action={saveAdminJurusan} className="grid gap-3 md:grid-cols-4"><select name="jurusanId" className={inputClass}>{jurusan.map(j=><option key={j.id.toString()} value={j.id.toString()}>{j.namaJurusan}</option>)}</select><input name="nama" placeholder="Nama admin" className={inputClass} required/><input name="username" placeholder="Username" className={inputClass} required/><input type="email" name="email" placeholder="Email" className={inputClass}/><input name="noHp" placeholder="No. HP" className={inputClass}/><select name="jenisKelamin" className={inputClass}><option value="">Jenis kelamin</option><option value="L">Laki-laki</option><option value="P">Perempuan</option></select><input type="password" name="password" placeholder="Password" className={inputClass} required/><button className={buttonClass}>Tambah Admin</button></form></Card><div className="mt-4 overflow-x-auto rounded-lg border bg-white"><table className="w-full text-sm"><thead className="bg-slate-50 text-left"><tr><th className="p-3">Nama</th><th className="p-3">Username</th><th className="p-3">Jurusan</th><th className="p-3">Aksi</th></tr></thead><tbody>{rows.map(r=><tr key={r.id.toString()} className="border-t"><td className="p-3 font-semibold">{r.nama}</td><td className="p-3">{r.user.username}</td><td className="p-3">{r.user.jurusan?.namaJurusan??"-"}</td><td className="p-3"><form action={deleteAdminJurusan}><input type="hidden" name="id" value={r.id.toString()}/><button className={dangerClass}>Hapus</button></form></td></tr>)}{!rows.length&&<EmptyRow colSpan={4}/>}</tbody></table></div></>}
