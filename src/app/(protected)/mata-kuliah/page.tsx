import{Card,EmptyRow,Notice,PageHeader,buttonClass,dangerClass,inputClass}from"@/components/admin-ui";import{requireManager}from"@/lib/admin/access";import{prisma}from"@/lib/prisma";import{deactivateMataKuliah,saveMataKuliah}from"../admin-actions";
export default async function Page({searchParams}:{searchParams:Promise<{notice?:string}>}){const a=await requireManager(),{notice}=await searchParams,where=a.role==="AdminJurusan"?{jurusanId:a.jurusanIdBigInt!}:undefined;const[rows,jurusan,semester,skema]=await Promise.all([prisma.mataKuliah.findMany({where,include:{jurusan:true,semester:{include:{semester:true}},skema:{include:{skema:true}}},orderBy:{kodeMk:"asc"}}),prisma.jurusan.findMany({where:a.role==="AdminJurusan"?{id:a.jurusanIdBigInt!}:undefined}),prisma.semester.findMany({orderBy:{id:"desc"}}),prisma.skema.findMany({where,orderBy:{namaSkema:"asc"}})]);return <><PageHeader title="Mata Kuliah" description="Relasi semester dan skema disimpan tanpa mengubah pilihan serta nilai mahasiswa lama."/><Notice text={notice}/><Card><form action={saveMataKuliah} className="grid gap-3 md:grid-cols-3"><select name="jurusanId" className={inputClass}>{jurusan.map(j=><option key={j.id.toString()} value={j.id.toString()}>{j.namaJurusan}</option>)}</select><input name="kodeMk" placeholder="Kode MK" className={inputClass} required/><input name="namaMk" placeholder="Nama mata kuliah" className={inputClass} required/><input type="number" min="1" name="sks" placeholder="SKS" className={inputClass} required/><input type="number" name="nilaiMinimum" placeholder="Nilai minimum" className={inputClass}/><label className="flex items-center gap-2 text-sm"><input type="checkbox" name="status" defaultChecked/> Aktif</label><fieldset><legend className="mb-1 text-xs font-semibold">Semester</legend>{semester.map(x=><label key={x.id.toString()} className="mr-3 text-sm"><input type="checkbox" name="semesterIds" value={x.id.toString()}/> {x.label}</label>)}</fieldset><fieldset><legend className="mb-1 text-xs font-semibold">Skema</legend>{skema.map(x=><label key={x.id.toString()} className="mr-3 text-sm"><input type="checkbox" name="skemaIds" value={x.id.toString()}/> {x.namaSkema}</label>)}</fieldset><button className={buttonClass}>Tambah Mata Kuliah</button></form></Card><div className="mt-4 overflow-x-auto rounded-lg border bg-white"><table className="w-full text-sm"><thead className="bg-slate-50 text-left"><tr><th className="p-3">Kode/Nama</th><th className="p-3">Jurusan</th><th className="p-3">SKS</th><th className="p-3">Semester/Skema</th><th className="p-3">Status</th><th className="p-3">Aksi</th></tr></thead><tbody>{rows.map(r=><tr key={r.id.toString()} className="border-t"><td className="p-3"><b>{r.kodeMk}</b><div>{r.namaMk}</div></td><td className="p-3">{r.jurusan.namaJurusan}</td><td className="p-3">{r.sks}</td><td className="p-3 text-xs">{r.semester.map(x=>x.semester.label).join(", ")||"-"}<br/>{r.skema.map(x=>x.skema.namaSkema).join(", ")||"-"}</td><td className="p-3">{r.status?"Aktif":"Nonaktif"}</td><td className="p-3">{r.status&&<form action={deactivateMataKuliah}><input type="hidden" name="id" value={r.id.toString()}/><button className={dangerClass}>Nonaktifkan</button></form>}</td></tr>)}{!rows.length&&<EmptyRow colSpan={6}/>}</tbody></table></div></>}
